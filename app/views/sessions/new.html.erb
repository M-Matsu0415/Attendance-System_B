<% provide(:title, 'login') %>
<h1>ログイン</h1>

<div class = "row">
    <div class = "col-md-6 col-md-offset-3">
        <%= form_with(scope: :session, url: login_path, local: true) do |f| %> <!-- local: true：HTMLとしてフォームを送信する-->
            
            <%= f.label :email, class: "label-login" %>
            <%= f.email_field :email, class: "form-control" %>
        
            <%= f.label :password, class: "label-login" %>
            <%= f.password_field :password, class: "form-control" %>
            
            <%= f.label :remember_me, class: "checkbox inline checkbox-login", for: "remember_me" do %>
            <!--inline:同じ水平行にチェックボックスをグループ化する-->
                <%= f.check_box :remember_me, id: "remember-me" %>
                <!--check_box:この場合、remember_meが登録先のカラム名-->
                <span>ログイン状態を記録する</span>
                <!--span:囲った部分をインライン要素としてグループ化することが出来る-->
            <% end %>
        
            <%= f.submit "ログイン", class: "btn btn-primary btn-block btn-signup" %>
            <!--btn-block：横一杯のブロックレベルのボタン-->
            <p>
              アカウント作成は<%= link_to "こちら", signup_path %>から
            </p>
        <% end %>
    </div>
</div>